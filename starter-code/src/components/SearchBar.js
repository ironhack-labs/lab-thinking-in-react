import React from 'react';

const SearchBar = () => {
    return(
        <form>
            <label>Search</label>
            <input type="search" placeholder="Type a product…" />
        </form>
    )
}

export default SearchBar;