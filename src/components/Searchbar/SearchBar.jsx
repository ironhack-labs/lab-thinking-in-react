import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <form>
        <input type="text" placeholder="search product" />
      </form>
    </div>
  );
};

export default SearchBar;
